# OS
.DS_Store
Thumbs.db

# 에디터 / 로컬 도구
.vscode/
.idea/
.claude/

# 배포 도구
.vercel/
.netlify/

# 의존성 (향후)
node_modules/
test-results/
playwright-report/
blob-report/

# 비밀값 — 절대 커밋 금지
.env
.env.*

# XcodeGen 생성물 — ios/ 에서 xcodegen generate 로 만든다 (커밋하지 않는다)
ios/*.xcodeproj/
ios/*.xcworkspace/
ios/TripCanvasFree-Info.plist
ios/*/Info.plist
ios/*/*.entitlements

# 예시 환경변수 파일은 올린다 (실제 값은 .env.local 등에 — 위 규칙으로 무시된다)
!.env.example
