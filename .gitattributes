# 리눅스(NAS·컨테이너)에서 그대로 읽히는 파일은 **LF로 체크아웃**한다.
# Windows에서 체크아웃하면 CRLF가 붙는데, docker compose의 .env 파서와 셸 스크립트는
# 줄 끝의 \r을 값·명령의 일부로 읽는다 — "unexpected character in variable name" 류로 터진다.
deploy/** text eol=lf
next/Dockerfile text eol=lf
*.sh text eol=lf
